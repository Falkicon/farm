# Local Development Environment Example
# Copy this file to .env.local and modify as needed for your local setup

# Database (Prisma)
# Replace with your local PostgreSQL credentials and preferred database names
DATABASE_URL=postgresql://your_username:your_password@localhost:5432/fabricmill_dev
TEST_DATABASE_URL=postgresql://your_username:your_password@localhost:5432/fabricmill_test_dev

# Development Server Ports
# Modify if you have port conflicts or prefer different ports
PORT=8000                    # Backend API port
VITE_DEV_SERVER_PORT=3000   # Frontend dev server port

# Logging
# Customize logging for local development
LOG_LEVEL=debug             # Options: debug, info, warn, error
LOG_FORMAT=pretty           # Options: pretty, json (pretty is better for local development)

# CORS
# Add any additional URLs you need for local development
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://your-local-domain:3000

# Optional: Development Conveniences
# Uncomment and modify these if needed for your local workflow
#VITE_DEV_SERVER_HOST=0.0.0.0    # Use this to access dev server from other devices
#VITE_HMR_HOST=localhost         # Hot Module Replacement host
#DATABASE_SSL=false              # Disable SSL for local database connections
#LOG_SQL_QUERIES=true           # Enable SQL query logging for debugging
